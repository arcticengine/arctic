language: cpp
sudo: required
dist: trusty
compiler:
- clang
os:
- linux
before_install:
- sudo apt-get install cmake libasound2-dev libglu1-mesa-dev freeglut3-dev
script: 
- cmake .
- make all
branches:
  only:
  - master

